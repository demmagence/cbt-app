rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default: deny all access (will be updated in Issue #11 & #54)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
